# Normalize line endings: store as LF, check out as LF everywhere.
# This ensures Python scripts run correctly on Raspberry Pi (Linux).
* text=auto eol=lf

# Binary files — no line-ending conversion
*.png binary
*.jpg binary
